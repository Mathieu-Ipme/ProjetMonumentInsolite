{#<header>#}
    <nav class="navbar navbar-expand-lg bg-body-tertiary mb-4" data-bs-theme="dark">
        <div class="container">
            <h1 >Constructions atypiques</h1>
            <div class="collapse navbar-collapse d-flex justify-content-between align-items-baseline" id="navbarNav">
                <a href="{{ path('app_home') }}" class="navbar-brand"></a>
                {% if(app.user) %}
                    <p>Bienvenue {{ app.user.name }}</p>
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_logout') }}">Se deconnecter</a>
                        </li>
                    </ul>
                {% else %}
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_register') }}">S'inscrire</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_login') }}">Se connecter</a>
                        </li>
                    </ul>
                {% endif %}
            </div>
        </div>
    </nav>
{#</header>#}
