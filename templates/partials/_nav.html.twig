<div class="example-wrapper">
    {#    <h1>Hello {{ controller_name }}! ✅</h1>#}



    {%  set genres = get_genres() %}

{#    <ul class="nav nav-tabs text-white ">#}
{#        <li class="nav-item">#}
{#           <a class="nav-link " aria-current="page" href="{{ path('app_home')}}">Acceuil</a>}#}
{#       </li>#}



{#        {% for genre in genres %}#}
{#        <li class="nav-item">#}
{#          <a class="nav-link " href="{{ path('app_genre', {'slug':genre.slug}) }}">{{genre.label}} </a>#}
{#        </li>#}
{#        {% endfor %}#}






           <div class="dropdown">


                <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
                    <div class="container-fluid">
                        <a class="navbar-brand" href="{{ path('app_home')}}">Accueil</a>
                        <a class="navbar-brand" href="#">Pays</a>
                        {%  if is_granted("ROLE_ADMIN") %}
                        <a class="navbar-brand" href="{{ path('app_admin_article') }}">Administration</a>
                        {% endif %}
                        <a class="navbar-brand" href="#">A propos</a>
                        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDarkDropdown" aria-controls="navbarNavDarkDropdown" aria-expanded="false" aria-label="Toggle navigation">
                            <span class="navbar-toggler-icon"></span>
                        </button>
                        <div class="collapse navbar-collapse" id="navbarNavDarkDropdown">
                            <ul class="navbar-nav">
                                <li class="nav-item dropdown">
                                    <button class="btn btn-dark dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
                                        Styles des monuments
                                    </button>
                                    <ul class="dropdown-menu dropdown-menu-dark">
                                        {% for genre in genres %}
                                        <li><a class="dropdown-item" href="{{ path('app_genre', {'slug':genre.slug}) }}">{{genre.label}}</a></li>
                                        {% endfor %}
                                    </ul>
                                </li>
                            </ul>
                        </div>
{#                       <a class="navbar-brand" href="#">Pays</a>#}
{#                       <a class="navbar-brand" href="#">A propos</a>#}

                    </div>
                </nav>
            </div>
    </ul>

</div>
